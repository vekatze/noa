import {
  gen {Gen, gen},
  suite {gen-kit},
  suite.combinator {draw-float},
}

inline gen-float(lo: float, hi: float, pivot: float) -> gen(float) {
  Gen(
    <r>(k: &gen-kit) =>> {
      draw-float<r>(k, lo, hi, pivot)
    },
  )
}
