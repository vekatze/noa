import {
  core::vector {from-list},
  gen {gen, gen-map},
  gen.list {gen-list},
}

define gen-vector<a>(elem: gen(a)) -> gen(vector(a)) {
  gen-map(gen-list(elem), (xs) => {
    from-list(xs)
  })
}
